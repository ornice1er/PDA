/*
import { JwtHelperService } from "@auth0/angular-jwt";
import {globalName} from './utils';

const helper = new JwtHelperService();

const decodedToken = helper.decodeToken(globalName.token);
const expirationDate = helper.getTokenExpirationDate(globalName.token);
export const isExpired = helper.isTokenExpired(globalName.token);*/
